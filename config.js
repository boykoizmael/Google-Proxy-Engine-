/*
  1117 backend configuration.

  LOCAL / RAILWAY:
    Leave this blank. The frontend will use /api/search on the same server.

  GITHUB PAGES:
    After your Railway deployment is live, replace the empty string with
    your Railway URL, for example:
    window.1117_API_BASE = "https://your-app.up.railway.app";
*/
window.1117_API_BASE = "";
